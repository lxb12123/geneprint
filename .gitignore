# Zero runtime dependencies — the engine ships pure Node + a tiny built-in
# YAML reader (lib/yaml-lite.mjs), so there is nothing to install and nothing
# to commit. node_modules (if a dev ever adds dev-deps) stays out of the repo.
node_modules/
